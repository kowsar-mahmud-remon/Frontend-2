import React from 'react';
import SearchBox from '../../components/SearchBox/SearchBox';

const index = () => {
    return (
        <div>
            <SearchBox></SearchBox>
        </div>
    );
};

export default index;